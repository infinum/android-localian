ext {
    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 30,
            "targetSdk" : 30,
            "buildTools": "30.0.3"
    ]
    releaseConfig = [
            "group"      : "com.infinum.localian",
            "version"    : "1.0.2",
            "versionCode": 1 * 100 * 100 + 0 * 100 + 2
    ]
}