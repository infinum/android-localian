ext {
    def major = 1
    def minor = 0
    def patch = 7

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 32,
            "targetSdk" : 32,
            "buildTools": "32.0.0"
    ]
    releaseConfig = [
            "group"      : "com.infinum.localian",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}