ext {
    def major = 1
    def minor = 1
    def patch = 0

    buildConfig = [
            "minSdk"    : 21,
            "compileSdk": 33,
            "targetSdk" : 33,
            "buildTools": "33.0.2"
    ]
    releaseConfig = [
            "group"      : "com.infinum.localian",
            "version"    : "$major.$minor.$patch",
            "versionCode": major * 100 * 100 + minor * 100 + patch
    ]
}