task deployLocalian(dependsOn: [
        ':localian:clean',
        ':localian:publishReleasePublicationToSonatypeRepository',
        ':localian:publishReleasePublicationToGithubRepository'
]) {
    group = "Deploy"
    description = "Deploy module to repositories"
}